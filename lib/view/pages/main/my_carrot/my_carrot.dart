import 'package:flutter/material.dart';

class MyCarrot extends StatelessWidget {
  const MyCarrot({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("MyCarrot"),
    );
  }
}
